# 本工作区强制约束（CT 肝脏肿瘤分割）

1. **Agent 只写代码与文档，不执行任务。** 所有实际执行（跑脚本、训练、推理、看数据）都由用户在远程平台手动完成。本地环境下不要尝试运行训练/推理/数据处理脚本，也不要把"我已经跑过并验证"当作事实。
2. **git 推送只在收到指示时执行。** 默认只改本地文件，不 `git add` / `commit` / `push`；用户明确说推送才做。远程侧 pull 同样由用户手动完成。
3. **`data/` 目录为空是正常的。** 真实数据只存在于远程平台，且受保密协议约束，不会出现在本地仓库。不要因为 `data/` 里只有占位文件就判定数据缺失、环境损坏或任务被阻塞，也不要尝试下载或生成数据来"补齐"。
4. **需要远程执行时，分两种情况。** （1）一次性代码：给出 `python - <<'PY' ... PY` 直接由用户粘贴至终端运行。（2）项目内持久化代码先在工作目录编写，由用户指示写 commit 然后 push 到 git，由用户在远程平台拉取代码后执行，编写持久化代码的用法时默认当前 Linux 工作目录为项目根目录。
5. **全程使用中文交流。** 与用户的对话、文档、代码注释、脚本输出说明、提交信息一律用中文（必要的技术术语、API 名、包名保持原文）。

背景、远程环境版本、编码约定等全部细节见 `docs/README.md`；本文件只放约束，不重复其内容。
